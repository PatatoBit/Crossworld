import googleSansFlex from "@fontsource-variable/google-sans-flex/files/google-sans-flex-latin-wght-normal.woff2?url";

/** Troika/Three.js font URL — same face as the UI sans stack. */
export const googleSansFontUrl = googleSansFlex;
